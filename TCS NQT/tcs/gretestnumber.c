#include<stdio.h>
int main()
{
    int num1=100,num2=30000;
    if(num1<num2)
    {
        printf("%d is gretest of two  number ",num2);
    }
    else{
        printf("%d is gretestof two number ",num1);
    }
    return 0;
}