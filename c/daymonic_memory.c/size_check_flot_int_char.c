# include<stdio.h>
int main()
{
	int *ptr;
	printf("the size of int of my pc is %d \n", sizeof(int));
	printf("the size of float of my pc is %d \n", sizeof(float));
	printf("the size of char of my pc is %d \n", sizeof(char));
	return 0;
}
